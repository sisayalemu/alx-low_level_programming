#include "main.h"

/**
 * mul - function to multply two numbers
 * @a: the first number
 * @b: the second number
 * Return: multiplication of two number
 */

int mul(int a, int b)
{
	return (a * b);
}
