#include "main.h"
/**
 * _islower - function for checking lowercase
 * @c: variable for integer
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
 
