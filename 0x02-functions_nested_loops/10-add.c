#include "main.h"
/**
 *add - this is a function that add 2 number
 *@x: the first variable being added
 *@y: the second variable being added
 *Return: Always 0 (success0
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;

	return (sum);
}
